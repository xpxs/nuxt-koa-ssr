const name = 'junnerblog'
const version = 'v0.0.1'
const api = 'api'
const timeout = 50000

export const WEBSITE = {
  name: name,
  api: api,
  baseApi: '/' + api,
  timeout: timeout,
  version: version
}